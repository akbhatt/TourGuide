<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TourListTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:paddingBottom">10dp</item>
        <item name="android:paddingStart">10dp</item>
        <item name="android:paddingEnd">10dp</item>
        <item name="android:textColor">@color/bitGold</item>
    </style>

    <style name="TourListWithNameWebsiteTypeTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:textColor">@color/justWhite</item>
        <item name="android:background">@color/food_blue</item>
        <item name="android:textSize">22sp</item>
        <item name="android:padding">@dimen/min_dimen</item>
        <item name="android:textColorLink">@color/justWhite</item>
    </style>

</resources>
